# 1:1 연락처 교환 한마디 — 24시간 대기

2026-09-20 코드 반영 준비 완료. 21:56 KST 운영 SQL Editor 읽기 검증으로
기존 함수가 이미 24시간임을 확인했다. 활성 트리거 1개와 참여자/결제/취소 보호 조건,
security definer/search_path 및 anon/authenticated 직접 실행 차단도 확인했다.
운영 함수를 다시 덮어쓰거나 기존 발송/매칭/결제 데이터를 변경하지 않았다.

## 변경

- 쌍방 수락 후 미결제 상태에서 한마디 보내기를 여는 시간을 48시간에서 24시간으로 단축한다.
- 앱/API는 공통 시간 상수를 사용하고, DB 삽입 검증도 같은 24시간을 적용한다.
- 기존 매칭에도 원래 쌍방 수락 시각을 기준으로 적용한다. 24~48시간 사이이며 아직 문구를 보내지 않은 매칭도 배포/DB 적용 후 목록을 다시 불러오면 대상이 된다.
- 자동 발송이 아니다. 회원이 기존 정해진 문구를 선택해 전송해야 알림/이메일이 전송된다.
- 동일 매칭에서 발신자별 1회 제한, 차단 확인, 참여자 확인, 결제/취소 상태 검사, 한글/이메일 문구는 유지한다.
- 번호 공개·매칭 취소의 48시간 규칙, 후보 새로고침, 결제 금액, 기존 발송 기록은 변경하지 않는다.

## 적용 순서

1. 기존 운영 DB에는 `supabase/sql/dating_1on1_contact_nudge_24h.sql` 실행. 기존 검증 함수의 시간 조건만 바꾸며 데이터 갱신/삭제/발송은 없다. 중복 실행 가능.
2. 이후 코드 배포. DB를 먼저 바꿔도 구버전 API는 48시간까지 계속 제한하므로 조기 발송이 일어나지 않는다.
3. 기존 회원은 매칭 목록을 다시 조회하면 새 조건을 적용받는다. 열어 둔 화면을 정확히 24시간 시점에 자동 갱신하는 변경은 아니다.

코드만 먼저 배포하고 SQL을 누락하면 24~48시간 매칭의 버튼은 떠도 DB가 발송을 거절한다. 따라서 두 가지가 모두 적용되기 전에는 운영 적용 완료라고 안내하지 않는다.
새 설치용 `dating_1on1_contact_nudges.sql`도 24시간으로 맞췄다.

## 검증

`CONTACT_NUDGE_PGLITE_PATH` 또는 `PRIVACY_TEST_PGLITE_PATH`를 독립 PGlite 설치 경로로 지정하고 `node --test scripts/check-dating-1on1-contact-nudge.cjs` 실행.

- 23:59:59.999 미허용, 정확히 24시간 허용, 24~48시간 허용.
- 실제 조회 API의 버튼 노출 조건 및 실제 전송 API의 상태/당사자/차단/중복/DB 경쟁 상태 검사.
- React 렌더링의 24시간 안내 및 기존 발송 기록 유지.
- 실제 PostgreSQL 엔진에서 기존 48시간 함수 → 새 함수 업그레이드, 중복 실행, 기존 기록 보존, 정확한 경계 시각, 양쪽 발신자별 1회, 결제/취소/비참여자/누락 매칭 차단, DB 직접 접근 권한 유지.
- 테스트에서는 운영 DB 변경, 실제 메시지/이메일 전송, 결제를 하지 않는다.
- 한마디 전용 34개 및 연령/후보 추천/새로고침 포함 회귀 182개 통과(건너뜀 0건). 수정 파일 린트 통과.
- 운영 기준 커밋 `62db96b`에 이번 수정만 복사한 분리 작업 폴더에서 프로덕션 빌드·타입 검사 통과(308개 페이지). 기존 middleware/Browserslist 경고는 유지.
