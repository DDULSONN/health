import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "1RM 계산기 | 짐툴 GymTools",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
