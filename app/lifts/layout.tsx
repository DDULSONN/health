import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "3대 합계 계산기 | 짐툴 GymTools",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

